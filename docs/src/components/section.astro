---
interface Props {
  eyebrow?: string;
  title?: string;
  description?: string;
  class?: string;
}

const { eyebrow, title, description, class: className = "" } = Astro.props;
---
<section class:list={["mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8", className]}>
  {(eyebrow || title || description) && (
    <div class="mb-8 max-w-3xl">
      {eyebrow && <p class="text-sm font-bold uppercase tracking-widest text-primary">{eyebrow}</p>}
      {title && <h2 class="mt-3 font-heading text-3xl font-extrabold tracking-tight text-foreground sm:text-4xl">{title}</h2>}
      {description && <p class="mt-4 text-lg leading-8 text-muted-foreground">{description}</p>}
    </div>
  )}
  <slot />
</section>
